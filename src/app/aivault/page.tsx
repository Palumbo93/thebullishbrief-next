"use client";

import { AIVault } from '../../page-components/AIVault';
import { Layout } from '../../components/Layout';

export default function AIVaultPageWrapper() {
  return (
    <Layout>
      <AIVault />
    </Layout>
  );
}
